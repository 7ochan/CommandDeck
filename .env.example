# The local server always defaults to the loopback interface.
COMMANDDECK_HOST=127.0.0.1
PORT=3000

# Optional: override the operating-system application data directory.
# COMMANDDECK_DATA_DIR=/absolute/path/to/commanddeck-data
